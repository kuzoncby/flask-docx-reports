{% extends "layouts/app.jinja2" %}

{% block title %}欢迎 | Laravel{% endblock %}

{% block content %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">控制台</div>

                    <div class="card-body">
                        {% if 'status' in session %}
                            <div class="alert alert-success">
                                {{ session['status'] }}
                            </div>
                        {% endif %}

                        您已登陆！
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
